# Development — Vite dev server proxies /api to the backend
# You do NOT need to set VITE_API_BASE_URL when using the Vite proxy (see vite.config.js)
# Uncomment the line below only if you are NOT using the Vite proxy:
# VITE_API_BASE_URL=http://localhost:3001
